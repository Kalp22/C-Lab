/*
    Given: a string of random numbers

    Divide the numbers in two equal parts
    Sort the first part in Ascending order and the next part in Descending order
    After sorting, find the indices of the numbers whose positions were not changed
*/

#include<bits/stdc++.h>

using namespace std;

int main(){
    string nums;
    cin >> nums;

    int size = nums.size();

    string left, right;
    return 0;
}